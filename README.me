# How to build.


1. `cd` into the project.
2. `mkdir build && cd build`
3. `cmake ..` and then `cmake --build .`

Or, if you are windows and have MinGW : 

`cmake -G "MinGW Makefiles"..` and then `mingw32-make` .


